# Dev: compose mounts ./server here; image only installs dependencies.
FROM node:20-alpine

WORKDIR /app/server

COPY server/package.json server/package-lock.json ./
RUN npm ci

ENV NODE_ENV=development

EXPOSE 5001

CMD ["npm", "run", "dev"]
