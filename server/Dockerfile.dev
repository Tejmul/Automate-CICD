FROM node:20-alpine

WORKDIR /app/server

# better-sqlite3 needs a toolchain on Alpine
RUN apk add --no-cache python3 make g++

COPY server/package.json server/package-lock.json ./
RUN npm ci

EXPOSE 5001

CMD ["npm", "run", "dev"]
